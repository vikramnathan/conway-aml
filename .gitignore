# ---- Raw & generated data (large; rebuild via preprocess/build scripts) ----
*.csv
*.json
*.zip
*.parquet
*.lmdb

# ---- Model checkpoints ----
*.pt
*.ckpt
checkpoints/

# ---- Result summaries (kept locally, not tracked in the remote) ----
*.txt

# ---- Python env & caches ----
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# ---- OS / editor cruft ----
.DS_Store
*.swp

# NOTE: .txt result summaries (baseline_results.txt, graph_results.txt,
# cluster_results_*.txt) are kept locally but NOT tracked. The findings live in
# the tracked reports: REPORT.md, README.md, RESULTS_SUMMARY.md.
